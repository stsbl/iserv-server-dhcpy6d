# dhcpy6d is disabled by default
RUN=yes

# LLT DUID generated by iservchk
DUID=$(cat /var/lib/iserv/server-dhcpy6d/duid)

ENABLE_DYNAMIC_PREFIX_FROM_DHCPCD=$([ -s /var/lib/iserv/config/ipv6-delegation-interfaces.list ] && echo yes || echo no)
